version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11

  build:
    commands:
      - echo "Installing dependencies"
      - pip install -r requirements.txt
      - echo "Running app"
      - python app.py

artifacts:
  files:
    - '**/*'
